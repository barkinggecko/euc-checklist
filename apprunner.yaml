version: 1.0 
runtime: python311 
build:
  commands:
    pre-build:
      - python -m ensurepip --upgrade
      - pip install -r requirements.txt
run:
  command: reflex run
  network: 
    port: 3000
    env: MY_APP_PORT  